# WebdoctorV

[![.NET](https://img.shields.io/badge/.NET-6.0+-512BD4?logo=dotnet)](https://dotnet.microsoft.com/)
[![License](https://img.shields.io/badge/license-MIT-blue.svg)](LICENSE)

**WebdoctorV** — универсальный сервис для мониторинга здоровья различных сервисов и протоколов с поддержкой метрик Prometheus, REST API и веб-интерфейса.

## 🚀 Основные возможности

- ✅ **Множество протоколов**: HTTP, HTTPS, MySQL, PostgreSQL, TCP, UDP, SSH
- ✅ **REST API** для получения статусов сервисов
- ✅ **Метрики Prometheus** на `/metrics`
- ✅ **Веб-интерфейс** с обновлением в реальном времени (SignalR)
- ✅ **Гибкая конфигурация** в формате HCL с поддержкой групп и вложенности
- ✅ **Retry логика** с настраиваемыми параметрами
- ✅ **Валидация ответов** для HTTP (body contains, regex)
- ✅ **Плагинная система** для расширения функциональности
- ✅ **Docker поддержка** с готовыми образами

## 📋 Поддерживаемые протоколы

| Протокол | Описание | Статус |
|----------|----------|--------|
| HTTP/HTTPS | Проверка веб-сервисов с валидацией ответов | ✅ Реализовано |
| MySQL | Проверка подключения и выполнение SQL запросов | ✅ Реализовано |
| PostgreSQL | Проверка подключения и выполнение SQL запросов | ✅ Реализовано |
| TCP | Проверка доступности порта | ✅ Реализовано |
| UDP | Проверка доступности UDP порта | ✅ Реализовано |
| SSH | Проверка подключения и выполнение команд | ✅ Реализовано |
| WebSocket | Проверка WebSocket соединений | 🔄 В планах |
| Redis | Проверка Redis сервера | 🔄 В планах |
| MongoDB | Проверка MongoDB | 🔄 В планах |
| DNS | Проверка DNS резолвинга | 🔄 В планах |
| ICMP/Ping | Проверка доступности хоста | 🔄 В планах |

## 🏃 Быстрый старт

### Локальный запуск

```powershell
# Клонирование репозитория
git clone https://github.com/your-org/webdoctorv.git
cd webdoctorv

# Запуск с примером конфигурации
dotnet run --project WebdoctorV -- --ConfigPath "examples/example-health.hcl"
```

### Docker

```powershell
# Запуск через docker-compose
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

### Конфигурация через переменные окружения

```powershell
$env:ConfigPath = "examples/example-health.hcl"
dotnet run --project WebdoctorV
```

## ⚙️ Конфигурация

Конфигурация выполняется в формате HCL (HashiCorp Configuration Language). Файл конфигурации можно передать через:
- Параметр командной строки `--ConfigPath`
- Переменную окружения `ConfigPath`
- Стандартный ввод (pipe)
- Файл `/app/config.hcl` (в Docker)

### Пример конфигурации

```hcl
interval = "30s"

group "web-services" {
  name = "Web Services"
  protocol = "https"
  host = "api.example.com"
  port = 443
  method = "GET"
  
  service "health" {
    name = "Health Check"
    timeout = "10s"
    path = "/health"
    response {
      status = 200
      body_contains = "ok"
    }
  }
  
  group "api" {
    name = "API Endpoints"
    path = "/api"
    
    service "v1" {
      path = "/v1/status"
      retry_count = 2
      retry_delay = "1s"
    }
  }
}

group "databases" {
  name = "Database Services"
  
  service "mysql-main" {
    name = "MySQL Main"
    protocol = "mysql"
    host = "db.example.com"
    port = 3306
    timeout = "10s"
    username = "monitor"
    password = "secret"
    database = "test"
    query = "SELECT 1"
    
    response {
      rows = 1
      columns = 1
    }
  }
  
  service "postgres-main" {
    name = "PostgreSQL Main"
    protocol = "postgresql"
    host = "pg.example.com"
    port = 5432
    username = "monitor"
    password = "secret"
    database = "postgres"
    query = "SELECT version()"
  }
}
```

### Параметры конфигурации

#### Общие параметры

- `interval` - интервал между проверками (например, "30s", "1m")
- `timeout` - таймаут для проверки (например, "10s")
- `retry_count` - количество повторных попыток при ошибке
- `retry_delay` - задержка между повторными попытками

#### HTTP/HTTPS параметры

- `method` - HTTP метод (GET, POST, PUT, DELETE и т.д.)
- `path` - путь к endpoint
- `response.status` - ожидаемый HTTP статус код
- `response.body_contains` - строка, которая должна содержаться в теле ответа
- `response.body_regex` - регулярное выражение для валидации тела ответа

#### MySQL/PostgreSQL параметры

- `username` - имя пользователя
- `password` - пароль
- `database` - имя базы данных
- `query` - SQL запрос для выполнения
- `response.rows` - ожидаемое количество строк
- `response.columns` - ожидаемое количество колонок

#### SSH параметры

- `username` - имя пользователя
- `password` - пароль (или используйте SSH ключи)
- `command` - команда для выполнения
- `response.exit_code` - ожидаемый код возврата

## 📡 API Endpoints

### GET `/health`

Проверка здоровья самого WebdoctorV.

**Ответ:**
```json
{
  "status": "healthy",
  "timestamp": "2024-01-01T12:00:00Z",
  "services": {
    "total": 10,
    "alive": 9,
    "dead": 1,
    "pending": 0
  }
}
```

### GET `/api/health`

Получение общего статуса всех сервисов.

**Ответ:** Аналогичен `/health`

### GET `/api/health/services`

Получение списка всех сервисов с их статусами.

**Ответ:**
```json
{
  "count": 10,
  "services": [
    {
      "serviceId": "web-services.health",
      "path": "web-services.health",
      "fullHttpPath": "/health",
      "protocol": "https",
      "name": "Health Check",
      "host": "api.example.com",
      "port": 443,
      "status": "alive",
      "duration": 45.2,
      "lastCheck": "2024-01-01T12:00:00Z",
      "error": null
    }
  ]
}
```

### GET `/api/health/services/{path}`

Получение статуса конкретного сервиса по пути.

**Пример:** `GET /api/health/services/web-services.health`

### GET `/api/health/summary`

Получение сводной статистики.

**Ответ:**
```json
{
  "total": 10,
  "alive": 9,
  "dead": 1,
  "pending": 0,
  "uptimePercent": 90.0,
  "averageDurationMs": 45.2,
  "lastUpdate": "2024-01-01T12:00:00Z"
}
```

## 📊 Метрики Prometheus

Метрики доступны на эндпоинте `/metrics`:

- `webdoctor_executable_item_count` - количество проверяемых элементов
- `webdoctor_item_status` - статус элемента (-1 = pending, 0 = dead, 1 = alive)
- `webdoctor_item_last_duration` - длительность последней проверки в миллисекундах
- `webdoctor_item_last_check_date` - timestamp последней проверки
- `webdoctor_checks_total` - общее количество проверок (с метками: proto, name, id, path, status)
- `webdoctor_service_uptime_percent` - процент uptime для сервиса

### Пример запроса метрик

```bash
curl http://localhost:5000/metrics
```

## 🖥️ Веб-интерфейс

WebdoctorV включает веб-интерфейс с обновлением в реальном времени через SignalR.

### Доступ к интерфейсу

Откройте в браузере: `http://localhost:5000`

### Возможности интерфейса

- 📊 Dashboard с общей статистикой
- 📋 Таблица всех сервисов с фильтрацией
- 🌳 Древовидное отображение иерархии сервисов
- 🔄 Обновление в реальном времени
- 🎨 Цветовая индикация статусов (зеленый/красный/желтый)
- 🔍 Фильтрация по статусу, протоколу, имени

## 🔌 Плагинная система

WebdoctorV поддерживает плагины для расширения функциональности без изменения основного кода.

### Быстрый старт с плагинами

1. Создайте проект библиотеки классов
2. Реализуйте интерфейс `IChecker`
3. Скомпилируйте в DLL
4. Поместите DLL в папку `plugins/`

### Документация

Подробная документация по разработке плагинов: [PLUGIN_DEVELOPMENT.md](PLUGIN_DEVELOPMENT.md)

### Пример плагина

```csharp
using WebdoctorV.Checkers;
using WebdoctorV.Models;

namespace WebdoctorV.Plugin.MyChecker;

public class MyChecker : IChecker
{
    public bool Supports(string protocol)
    {
        return protocol.Equals("myprotocol", StringComparison.OrdinalIgnoreCase);
    }

    public async Task<CheckResult> CheckAsync(ServiceConfig service, string path)
    {
        // Ваша логика проверки
        return new CheckResult { Status = CheckStatus.Alive };
    }
}
```

## 🐳 Docker

### Использование готового образа

```powershell
docker run -d \
  -p 5000:80 \
  -v ./config.hcl:/app/config.hcl:ro \
  webdoctorv:latest
```

### Сборка образа

```powershell
docker build -t webdoctorv:latest .
```

### Docker Compose

```yaml
version: '3.8'
services:
  webdoctorv:
    build: .
    ports:
      - "5000:80"
    volumes:
      - ./config.hcl:/app/config.hcl:ro
      - ./plugins:/app/plugins:ro
    environment:
      - ConfigPath=/app/config.hcl
```

Подробнее: [DOCKER.md](DOCKER.md)

## 📁 Структура проекта

```
webdoctorv/
├── WebdoctorV/              # Основное приложение
│   ├── Checkers/            # Реализации чекеров
│   ├── Controllers/          # API контроллеры
│   ├── Models/              # Модели данных
│   ├── Parsers/              # Парсеры конфигурации
│   ├── Services/             # Сервисы
│   └── Hubs/                 # SignalR хабы
├── frontend/                 # React фронтенд
├── examples/                 # Примеры конфигураций
├── plugins/                  # Папка для плагинов (создается автоматически)
├── PLUGIN_DEVELOPMENT.md     # Документация по плагинам
├── TODO.md                   # Список задач
└── README.md                 # Этот файл
```

## 🧪 Тестирование

```powershell
# Запуск unit тестов
dotnet test

# Запуск с покрытием
dotnet test /p:CollectCoverage=true
```

## 📚 Дополнительная документация

- [PLUGIN_DEVELOPMENT.md](PLUGIN_DEVELOPMENT.md) - Руководство по разработке плагинов
- [DOCKER.md](DOCKER.md) - Документация по Docker
- [TODO.md](TODO.md) - Список задач и планов развития
- [examples/](examples/) - Примеры конфигураций

## 🛠️ Разработка

### Требования

- .NET 6.0 или выше
- PowerShell (для скриптов)

### Сборка

```powershell
dotnet build
```

### Запуск в режиме разработки

```powershell
dotnet run --project WebdoctorV
```

### Запуск с отладкой

```powershell
dotnet run --project WebdoctorV -- --ConfigPath "examples/example-health.hcl"
```

## 🤝 Вклад в проект

Мы приветствуем вклад в развитие проекта! 

1. Fork репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

### Создание плагинов

Если вы хотите добавить поддержку нового протокола, рассмотрите возможность создания плагина вместо изменения основного кода. Это упростит поддержку и распространение.

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🙏 Благодарности

- [Prometheus](https://prometheus.io/) - за метрики
- [HashiCorp](https://www.hashicorp.com/) - за формат конфигурации HCL
- Всем контрибьюторам проекта

## 📞 Поддержка

- 🐛 [Issues](https://github.com/your-org/webdoctorv/issues) - Сообщить о баге или предложить функцию
- 💬 [Discussions](https://github.com/your-org/webdoctorv/discussions) - Обсуждения и вопросы
- 📧 Email: support@webdoctorv.example.com

---

**Сделано с ❤️ для мониторинга вашей инфраструктуры**
